<%= link_to "Draft Ideas", root_path, :class => 'brand' %>
<ul class="nav pull-right">
	<% if user_signed_in? %>
	  <li><%= link_to "Rascunhos de Idéias", drafts_path %></li>
		<li class="dropdown">
	    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.login %><b class="caret"></b></a>
	    <ul class="dropdown-menu">
	      <li><%= link_to "Logout", signout_path, :method => :delete %></li>
	    </ul>
	  </li>
	<% else %>
		<li><%= link_to("Login", signin_path) %></li>
		<li><%= link_to("Cadastre-se", "#") %></li>
	<% end %>
</ul>