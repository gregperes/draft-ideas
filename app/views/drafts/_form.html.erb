<%= simple_form_for @draft do |f| %>
  <% if @draft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@draft.errors.count, "error") %> prohibited this draft from being saved:</h2>
      <ul>
      <% @draft.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.input :title, label: "Título" %>
  <%= f.input :description, label: "Descrição" %>
  <%= f.input :tags, label: "Tags" %>
  <div class="control-group">
    <label class="control-label" for="">Usu&aacute;rio</label>
    <div class="controls">
      <span><%= current_user.login %></span>
    </div>
  </div>
  <%= f.submit 'Save', class: "btn btn-primary" %>
<% end %>